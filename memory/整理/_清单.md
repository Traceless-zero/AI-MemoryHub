# HMA 定时整理 · 检索路径清单
# 每行一个「检索路径」（glob 模式，相对仓库根；目录则递归读取其下 .md）。
# 脚本照路径找出文件 → 提炼 → 统一写入 memory/用户/用户数据.md 的「定时整合」段落（每周刷新）。
# 不在清单里写死具体文件：AI/脚本按路径去找。
# daylog 近 7 天由脚本固定处理（提炼偏好/厌恶 → 用户数据.md，并汇总 → 整理.md），不在此列。

# 各项目里的约定与纪律：每个具体项目文件夹下的 约定.md
memory/项目/*/约定.md

# 各文章 / 资料的理解：综述包所在目录（递归）
memory/其他
