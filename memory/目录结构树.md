# memory/ 目录结构树

> 由 `python -m hma.engine tree` 生成（派生缓存，等同 index.db，可由 .md 全量重建）。
> 只到「包 / 主题」层级；包内的 `.md` 事件文件不在此列出，只在 index.db 检索（L1 → L2 → L3）。

- `其他/`  文章 / 杂项 / 资料文本

- `原创角色/`  OC 角色档案

- `日志/`  每日叙事日志

- `用户/`  用户数据包（偏好 / 身份锚点 / 个人事实）
  - `用户/`  HMA 项目开发待办清单——索引 GUI 重做（已完）、AI 切换 HMA 验证 checklist（待跑）、HM…

- `项目/`  用户项目包
  - `项目/demo-mdnote/`  mdnote 检索管线与存储选型（共 5 项）
  - `项目/hma-design-journal/`  HMA 用户/项目信息的模块化划分、凝聚性拆包触发铁律、AI 收录时的模块路由判定（共 6 项）
